[package]
name = "kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
bootloader = "0.9.29"
x86_64 = { version = "0.14", default-features = false, features = ["instructions", "abi_x86_interrupt"] }
